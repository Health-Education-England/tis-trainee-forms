application:
  schedules:
    publish-all-ltfts: "-"
  signature:
    secret-key: test-secret-key

mongock:
  enabled: false

spring:
  config:
    import: file:src/main/resources/application.yml

cloud:
  aws:
    sqs:
      enabled: false
    region:
      static: eu-west-2

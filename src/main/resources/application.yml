server:
  port: 8207
spring:
  data:
    mongodb:
      host: ${DB_HOST:localhost}
      port: ${DB_PORT:27017}
      database: ${DB_NAME:forms}
      user: ${DB_USER:''}
      password: ${DB_PASSWORD:''}
